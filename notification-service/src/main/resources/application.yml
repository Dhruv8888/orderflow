spring:
  application:
    name: notification-service

  rabbitmq:
    host: localhost
    port: 5672
    username: orderflow
    password: orderflow

server:
  port: 8084

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus